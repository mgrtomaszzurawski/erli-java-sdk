package io.github.mgrtomaszzurawski.erli.domain.shop;

/** Whether the shop matches its offers to Erli's catalog externally. Mirrors the API enum. */
public enum MatchingPolicy {
    ENABLED,
    DISABLED
}
