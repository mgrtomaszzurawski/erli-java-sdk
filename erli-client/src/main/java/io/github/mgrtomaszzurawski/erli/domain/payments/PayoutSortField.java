package io.github.mgrtomaszzurawski.erli.domain.payments;

/** Field a payout search is ordered by. */
public enum PayoutSortField {

    /** The payout id. */
    ID,
    /** When the payout was created. */
    CREATED_AT,
    /** The payout amount. */
    AMOUNT
}
